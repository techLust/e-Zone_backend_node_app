// const data = require('./task-definition.json');

// for(let [key, value] of Object.entries(data)){

//     console.log(key);

// }

const backfire = require('firestore-backfire');
const exportData = require('exportFirestoreData');
